name = "booktalk-cover-proxy"
main = "src/worker.js"
compatibility_date = "2024-11-01"

# PROXY_TOKEN은 코드/설정에 넣지 말고 시크릿으로 주입:
#   wrangler secret put PROXY_TOKEN
# (백엔드의 COVER_PROXY_TOKEN과 동일한 값을 사용)
